---
description: 需要打包的时候
alwaysApply: true
---
# 每次构建和对话
- 每次修改记得自动重新构建插件包，不用问我要不要构建
- 每次修改任何代码都要重新构建，每次构建插件包记得更新插件包的版本号，小修改重复修改都要修改版本号
- 不要用cmd /c执行命令
- 每次打包后记得核对打包文件是否存在
- 构建前必须检查 build.gradle.kts 中的 version 字段并递增最后一位数字
- 构建完成后必须验证 build/distributions/ 目录下生成了对应版本的 zip 文件
- 如果忘记更新版本号，构建会被拒绝或警告